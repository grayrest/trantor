provides_symbol = "roc_main"
provided_fn = "main_for_host!"
requires_uses = ["Stdout", "Stderr"]
imports_extra = ["IOErr"]

requires = """
		main! : {} => Try({}, [Exit(I32), ..])
"""

provided = """
main_for_host! : () => I32
main_for_host! = || {
	match main!({}) {
		Ok({}) => 0
		Err(Exit(code)) => code
		Err(_) => 1
	}
}
"""
