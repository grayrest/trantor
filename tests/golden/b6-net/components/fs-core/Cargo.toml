[package]
name = "fs-core"
version = "0.0.0"
edition = "2021"
publish = false
[lib]
crate-type = ["rlib"]
[dependencies]
hematite-abi = { path = "../../abi" }
sync-io-core = { path = "../sync-io-core" }

[dependencies.paste]
version = "1"
