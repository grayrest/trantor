[package]
name = "rusqlite-host"
version = "0.0.0"
edition = "2021"
publish = false
[lib]
crate-type = ["staticlib", "rlib"]
[dependencies]
hematite-abi = { path = "../../abi" }
rusqlite = { version = "0.32", features = ["bundled"] }
