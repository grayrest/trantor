target/
bin/
glue-out/
# glue output (regenerated by build.sh)
abi/src/generated.rs
# verbatim copies trantor makes into platform/ (sources live in interfaces/ + components/pathlib/)
platform/Fs.roc
platform/Stdio.roc
platform/Env.roc
platform/IOErr.roc
platform/Path.roc
platform/targets/*/*.a
platform/targets/macos-sysroot/
dist/
