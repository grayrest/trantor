provides_symbol = "roc_main"
provided_fn = "main_for_host!"
requires_uses = ["Streams", "Memory", "FileIo"]
imports_extra = ["IOErr"]
requires = """
		run! : {} => Try({}, [Exit(I32), ..])
"""
provided = """
main_for_host! : () => I32
main_for_host! = || {
	match run!({}) {
		Ok({}) => 0
		Err(Exit(code)) => code
		Err(_) => 1
	}
}
"""
