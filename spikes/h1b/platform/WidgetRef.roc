import Widget exposing [Widget]
## A type ALIAS re-exporting another module's nominal -- the shape D13 binding
## modules would produce if they aliased rather than re-exported the nominal.
WidgetRef : Widget
